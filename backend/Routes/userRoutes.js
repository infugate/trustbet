// const express = require('express');
// const { getUserNameById } = require('../controllers/userController');
// const { authenticateToken } = require('../middekware/authMiddleware');

// const router = express.Router();

// // Route to fetch username by ID (protected)
// router.get('/name/:id', authenticateToken, getUserNameById);

// module.exports = router;
